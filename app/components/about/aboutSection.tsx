'use client'

import 'swiper/css'
import 'swiper/css/pagination'
import 'swiper/css/navigation'

export function AboutSection() {
  return (
    <div className="mockup-code w-full">
      <pre data-prefix="$" className="text-success"><code>What even is FilmScape?</code></pre>
      <pre data-prefix=">"><code>🎬 FilmScape is a solo passion project, built as part of a modern dev portfolio. It pulls data from [The Movie Database (TMDB)](https://www.themoviedb.org/) to show you all the spicy info about movies & TV shows — from what’s trending to what’s dropping soon.</code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix="$" className="text-success"><code>Current Features (aka what’s already cooking):</code></pre>
      <pre data-prefix=">" className="text-warning"><code>✅ Explore & filter by:</code></pre>
      <pre data-prefix=">"><code>   - Now Playing, Top Rated, Trending, Upcoming (Movies)</code></pre>
      <pre data-prefix=">"><code>   - Airing Today, On The Air, Trending, Top Rated (Series)</code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix=">" className="text-warning"><code>✅ Detailed pages for each movie/show:</code></pre>
      <pre data-prefix=">"><code>   - Overview / synopsis</code></pre>
      <pre data-prefix=">"><code>   - User rating</code></pre>
      <pre data-prefix=">"><code>   - Cast info</code></pre>
      <pre data-prefix=">"><code>   - Trailer (via YouTube iframe)</code></pre>
      <pre data-prefix=">"><code>   - Similar & recommended titles</code></pre>
      <pre data-prefix=">"><code>   - For series: full season breakdown</code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix=">" className="text-warning"><code>✅ Read reviews & ratings from real users (via TMDB)</code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix="$" className="text-success"><code>Tech Stack / The Brains Behind the Banger:</code></pre>
      <pre data-prefix=">" className="text-warning"><code>🛠️ Built using:</code></pre>
      <pre data-prefix=">"><code>   - React + TypeScript</code></pre>
      <pre data-prefix=">"><code>   - TailwindCSS + DaisyUI</code></pre>
      <pre data-prefix=">"><code>   - Headless UI</code></pre>
      <pre data-prefix=">"><code>   - React Router</code></pre>
      <pre data-prefix=">"><code>   - TMDB API</code></pre>
      <pre data-prefix=">"><code>   - Vite</code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix="$" className="text-success"><code>Upcoming Features / Roadmap-ish:</code></pre>
      <pre data-prefix=">" className="text-warning"><code>🚧 What’s next:</code></pre>
      <pre data-prefix=">"><code>   - 🔐 Login & user authentication</code></pre>
      <pre data-prefix=">"><code>   - ✍️ Users can leave reviews & ratings</code></pre>
      <pre data-prefix=">"><code>   - 👤 Profile page with watch history</code></pre>
      <pre data-prefix=">"><code>   - 💾 Save favs like a playlist of bangers</code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix=""><code></code></pre>
      <pre data-prefix="$" className="text-error"><code>🙏 Thanks for checking out FilmScape!</code></pre>
      <pre data-prefix=">" className="text-error"><code>This project was made with way too much coffee by a movie-loving indie dev. The goal? Learn, build, and vibe. If you got feedback or wanna collab, hit me up via github.</code></pre>
      <pre data-prefix=""><code></code></pre>
    </div>
  )
}
